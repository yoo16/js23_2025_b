// TODO: uuid インポート（エイリアス）: v4
// const { v4: uuidv4 } = require('uuid');

// TODO: UUID生成: uuidv4
const id = "";
const name = "Alice";
const email = "alice@test.com";

// TODO: オブジェクトリテラルで代入
const user = {};
console.table(user);